import 'package:flutter/material.dart';

class Strm1 extends StatelessWidget {
  const Strm1({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Scaffold();
  }
}
